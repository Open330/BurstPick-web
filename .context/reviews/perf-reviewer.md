# Performance Review — Cycle 3 (2026-04-26)

## Confirmations
- `<img>` → `next/image` migration complete.
- `priority` set on Header logo (above-the-fold).
- Hero background AVIF preloaded via `<link rel="preload" as="image">` in locale layout.
- Plus-Jakarta weight set still includes `500` (Plan 015 deferred).

## Outstanding (already tracked)
- Plan 015 — drop unused `500` weight.
- AGG2-5 (Plan 021) — ~22 hero mockup images; needs measurement before refactor.
- AGG-30 (Plan 018) — `transition-all` on cards.

## New findings
None.

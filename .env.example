# Optional. Set when serving from a non-root path (e.g. /docs).
NEXT_PUBLIC_BASE_PATH=

# Optional. Override the BurstPick API base URL.
# Defaults to https://api.burstpick.app when unset.
NEXT_PUBLIC_API_BASE_URL=
